# Use Node.js 20 LTS on Alpine
FROM node:20-alpine

# Set the working directory inside the container
WORKDIR /usr/src/app

# Copy the dist directory to the working directory
COPY dist /usr/src/app

# Expose the port on which your app runs
EXPOSE 3000

# Command to run the app using the entry point generated by ncc
CMD ["node", "index.js"]
